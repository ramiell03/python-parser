A Mathematical Expression Compiler API + UI

System Components:
Core Engine (Compiler)
REST API (Backend)
Swagger Documentation
Simple UI (Frontend)
🔷 🔹 TECHNOLOGY DECISIONS (FIXED)
Language: Python
API Framework: FastAPI (best for Swagger auto-doc)
UI: Simple HTML (or Streamlit if you want faster)
Docs: Swagger (auto-generated by FastAPI)


🔷 🔹 CORE FOUNDATION 

✅ 1. Token Specification (FINAL)
Token Types:
NUMBER
PLUS      # +
MINUS     # -
MULTIPLY  # *
DIVIDE    # /
LPAREN    # (
RPAREN    # )
EOF       # End of input
Token Class (FINAL)
class Token:
    def __init__(self, type: str, value=None):
        self.type = type
        self.value = value
Example:
Input: "3 + 5"

Output:
[
  Token(NUMBER, 3),
  Token(PLUS, '+'),
  Token(NUMBER, 5),
  Token(EOF, None)
]

✅ 2. Grammar (FINAL VERSION)
Expression → Term ((+ | -) Term)*
Term → Factor ((* | /) Factor)*
Factor → NUMBER | '(' Expression ')'
Rules:
Precedence: * / > + -
Associativity: Left-to-right

✅ 3. AST Structure (FINAL)
class NumberNode:
    def __init__(self, value):
        self.value = value

class BinaryOpNode:
    def __init__(self, left, operator, right):
        self.left = left
        self.operator = operator
        self.right = right

✅ 4. Interface Contracts (FINAL)
def tokenize(input: str) -> list:
    pass

def parse(tokens: list) -> object:
    pass

def evaluate(ast) -> float:
    pass
🔷 🚀 FULL PROJECT STEPS 


🧩 STEP 1: Project Setup
Tasks:
Create GitHub repo
Create folder structure
Add .gitignore and README


🧩 STEP 2: Tokenizer (Lexical Analyzer)
Implement:
Read input string
Identify tokens
Ignore spaces
Handle numbers (int + float)
Output:

List of Token objects

Also:
Raise error for invalid characters


🧩 STEP 3: Parser (Syntax Analyzer)
Implement:

Recursive descent parser:

parse_expression()
parse_term()
parse_factor()
Responsibilities:
Follow grammar strictly
Build AST
Detect syntax errors


🧩 STEP 4: AST Implementation
Implement:
Node classes
Tree structure
Bonus (Master level):
Add function to print or visualize tree


🧩 STEP 5: Evaluator
Implement:
Traverse AST recursively
Perform operations
Handle:
Division by zero
Invalid operations


🧩 STEP 6: Error Handling System
Create:
class LexerError(Exception)
class ParserError(Exception)
class EvaluationError(Exception)
Use in:
Tokenizer
Parser
Evaluator


🧩 STEP 7: Integration (Core Engine)
Build pipeline:
Input → Tokenize → Parse → AST → Evaluate → Result
Create:
def process_expression(expression: str):


🧩 STEP 8: API (FastAPI)
Install:
pip install fastapi uvicorn
Create API:
from fastapi import FastAPI

app = FastAPI()

@app.post("/evaluate")
def evaluate_expression(expression: str):
    return result


Endpoints:
1. Evaluate Expression
POST /evaluate

Request:

{
  "expression": "3 + 5 * 2"
}

Response:

{
  "result": 13
}
2. Get Tokens (optional but strong)
POST /tokens
3. Get AST (VERY IMPRESSIVE)
POST /ast


🧩 STEP 9: Swagger Documentation

👉 Automatically available at:

http://127.0.0.1:8000/docs
What to do:
Add descriptions to endpoints
Add examples
Add response models


🧩 STEP 10: UI (Frontend)
Option 1 (Simple HTML):
Input field
Button
Display result
Option 2 (Recommended): Streamlit
pip install streamlit
import streamlit as st

expr = st.text_input("Enter expression")

WE WILL USE STREAMLIT


🧩 STEP 11: Testing
Write:
Valid cases
Invalid cases
Edge cases


🧩 STEP 12: Documentation (REPORT)

Include:

System architecture
Grammar explanation
API documentation
Screenshots (Swagger + UI)


 FINAL SYSTEM ARCHITECTURE
        ┌──────────────┐
        │     UI       │
        └──────┬───────┘
               │
        ┌──────▼───────┐
        │   FastAPI    │
        │ (Swagger UI) │
        └──────┬───────┘
               │
        ┌──────▼───────┐
        │ Core Engine  │
        │              │
        │ Tokenizer    │
        │ Parser       │
        │ AST          │
        │ Evaluator    │
        └──────────────┘
🔥 FINAL DELIVERABLES (MASTER LEVEL)

We will present:

✅ Working API
✅ Swagger documentation
✅ UI interface
✅ Clean compiler pipeline
✅ Error handling
✅ Well-written report

